{
  "name": "dan-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "DAN - Strava for Studying",
  "workspaces": [
    "web",
    "extension",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev:web": "cd web && npm run dev",
    "dev:backend": "cd backend && npm run serve",
    "build:web": "cd web && npm run build",
    "build:extension": "cd extension && npm run build",
    "build:all": "npm run build:web && npm run build:extension",
    "install:all": "npm install && cd web && npm install && cd ../backend && npm install && cd ../extension && npm install && cd ../shared && npm install",
    "clean": "rm -rf node_modules web/node_modules extension/node_modules backend/node_modules shared/node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

